<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit</title>
    <link rel="stylesheet" href="/css/edit.css" />
  </head>
  <body>
    <div class="container">
      <% if(!locals.previousPost) {%>

      <form action="/post" method="POST">
        <label for="fancy-input">Author</label>
        <input
          type="text"
          id="fancy-input"
          name="author"
          placeholder="Your Name"
          required
        />
        <label for="fancy-input">Title</label>
        <input
          type="text"
          id="fancy-input"
          name="title"
          placeholder="Your Name"
          required
        />
        <label for="fancyTextarea">Blog</label>
        <textarea
          id="fancyTextarea"
          placeholder="Type something fancy..."
          required
          name="blog"
        ></textarea>
        <div class="container">
          <button type="submit" class="fancy-button">Post</button>
          <button class="fancy-button"><a href="/">Home</a></button>
        </div>
      </form>

      <% } else {%>
      <form action="/post/update/<%=previousPost.id%>" method="POST">
        <label for="fancy-input">Author</label>
        <input
          type="text"
          id="fancy-input"
          name="author"
          placeholder="Your Name"
          required
          value="<%= previousPost.author %>"
        />
        <label for="fancy-input">Title</label>
        <input
          type="text"
          id="fancy-input"
          name="title"
          placeholder="Your Name"
          required
          value="<%= previousPost.title %>"
        />
        <label for="fancyTextarea">Blog</label>
        <textarea
          id="fancyTextarea"
          placeholder="Type something fancy..."
          required
          name="blog"
        >
<%= previousPost.blog %> </textarea
        >
        <div class="container">
          <button type="submit" class="fancy-button">Post</button>
          <button class="fancy-button">Home</button>
        </div>
      </form>
      <% } %>
    </div>
  </body>
</html>
